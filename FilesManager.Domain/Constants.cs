namespace FilesManager.Domain
{
    public static class Constants
    {
        public const string SELECT_LIST = "SELECT * FROM c WHERE c.user_id = '{0}'";
        public const string CONTAINER = "files";
    }
}
