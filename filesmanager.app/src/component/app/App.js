import './App.css';
import { signIn, signOut } from '../../authRedirect';
import { getFiles } from '../../api/files/filesClient';
import Router from '../../Router';

function App() {
  return (
    <Router />
  );
}

export default App;
